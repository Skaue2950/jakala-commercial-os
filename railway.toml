[build]
builder = "nixpacks"

[deploy]
startCommand = "cd web && python seed_db.py && cd .. && gunicorn --chdir web app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
