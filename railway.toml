[build]
builder = "nixpacks"

[deploy]
startCommand = "cd web && python -c \"from seed_db import seed; seed(force=True)\" && cd .. && gunicorn --chdir web app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
