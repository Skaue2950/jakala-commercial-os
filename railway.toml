[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --chdir web app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
